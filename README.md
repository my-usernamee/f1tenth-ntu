# F1TENTH NTU Website

A minimal responsive static website for an F1TENTH club at NTU.

## Files

- `index.html` - page structure and content
- `styles.css` - responsive visual design
- `script.js` - mobile navigation toggle

## Host on GitHub Pages

1. Create a new GitHub repository, for example `f1tenth-ntu`.
2. Add these files to the root of the repository.
3. Commit and push to the `main` branch.
4. On GitHub, open the repository and go to `Settings` > `Pages`.
5. Under `Build and deployment`, choose `Deploy from a branch`.
6. Select branch `main` and folder `/(root)`, then save.

Your site should be available at:

```text
https://<your-github-username>.github.io/f1tenth-ntu/
```

If you create a repository named `<your-github-username>.github.io`, the site will be available at:

```text
https://<your-github-username>.github.io/
```

Before publishing, replace `your-email@ntu.edu.sg` in `index.html` with the club email, form link, or social link you want students to use.
